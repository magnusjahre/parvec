#!/bin/bash

# Run Scalar
#bash parsecmgmt -a run -p parsec.simd.nqueens -c gcc-hooks -i simlarge

# Run SSE
bash parsecmgmt -a run -p parsec.simd.nqueens -c gcc-sse-hooks -i simlarge

# Run AVX
bash parsecmgmt -a run -p parsec.simd.nqueens -c gcc-avx2-hooks -i simlarge