#!/bin/bash

source ${PARSECDIR}/pkgs/tools/nasm/parsec/icc.bldconf

# Package dependencies
# NOTE: nasm itself does not depend on hooks but the
# library is added globally and hence we must add it
build_deps="hooks"
