#!/bin/bash

# gcc-tbb.bldconf - configuration file for PARSEC

source ${PARSECDIR}/pkgs/tools/nasm/parsec/gcc.bldconf

# Package dependencies
# NOTE: nasm itself does not depend on tbb but the
# library is added globally and hence we must add it
build_deps="tbb"

