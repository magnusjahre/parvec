#!/bin/bash

# gcc-pthreads.bldconf - configuration file for PARSEC

source ${PARSECDIR}/pkgs/tools/automake/parsec/gcc-pthreads.bldconf

# Package dependencies
# NOTE: automake itself does not depend on hooks but the
# library is added globally and hence we must add it
build_deps="hooks"

