#!/bin/bash

# gcc-tbb.bldconf - configuration file for PARSEC

source ${PARSECDIR}/pkgs/tools/automake/parsec/gcc.bldconf

# Package dependencies
# NOTE: automake itself does not depend on tbb but the
# library is added globally and hence we must add it
build_deps="tbb"

