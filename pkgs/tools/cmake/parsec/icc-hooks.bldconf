#!/bin/bash

source ${PARSECDIR}/pkgs/tools/cmake/parsec/icc.bldconf

# Package dependencies
# NOTE: cmake itself does not depend on hooks but the
# library is added globally and hence we must add it
build_deps="hooks"
